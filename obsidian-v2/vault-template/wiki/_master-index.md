# Wiki — evergreen knowledge

Post-ship harvest target. Articles are slug-only (no date prefix) and must include
a `## Key Takeaways` section. Create topic folders as they emerge; give each its own `_index.md`.
Populated by `/harvest <project-file>` after a project ships.
